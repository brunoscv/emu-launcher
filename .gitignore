# Node
node_modules/
dist/
dist-ssr/
*.local

# Fotos dos consoles do carrossel — procedência do Bruno, não redistribuídas
# pelo repositório (ver public/consoles/README.md)
public/consoles/*
!public/consoles/README.md

# Rust / Tauri
src-tauri/target/
src-tauri/gen/

# Variáveis de ambiente — nunca commitar segredo de verdade
.env
.env.*
!.env.example

# Editor / SO
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.log

# Estado local do Claude Code (agendamento de sessão, etc.)
.claude/
